"""Base declarativa de SQLAlchemy 2.x para todos los modelos ORM."""

from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """Base común de todos los modelos ORM (estilo SQLAlchemy 2.x)."""

    pass
